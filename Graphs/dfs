/***
Input: 
5 7
0 1
0 4
1 2
1 3
1 4
2 3
3 4

Output: 0 1 2 3 4 
***/

#include <bits/stdc++.h>
#include<iostream>
using namespace std;

void dfs(int node ,int V, vector<int>adj[], vector<int>&vis,vector<int>&storeDfs ){
   
    
    storeDfs.push_back(node);
    vis[node]=1;
    for(auto it: adj[node]){
        if(!vis[it]){
        dfs(it, V, adj,vis,storeDfs);
        }
    }
}

int main() {
	int V , e;
	cin >> V  >> e;
	vector<int> adj[V];//vector of array to store edges
    vector<int> vis(V,0);
     vector<int> storeDfs;
      
	for(int i = 0 ; i<e ; i++){
	    int u,v;
	    cin >> u >>v;
	    
	    adj[u].push_back(v);
	    adj[v].push_back(u);
	}

        dfs(0, V, adj,vis,storeDfs);
       for(int i = 0 ; i <storeDfs.size(); i++)
       {
           cout << i << " ";
       }
        cout << endl;
	
	return 0;
}
